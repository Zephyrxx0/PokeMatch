services:
  - type: web
    name: pokemon-api
    runtime: python
    buildCommand: pip install -r python/requirements.txt
    startCommand: gunicorn --chdir python/api server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
